# CLAUDE.md

This repository contains the AI Care Report backend and frontend projects.

## Project layout

- `BE/`: backend application
- `FE/`: frontend application
- `.github/`: GitHub repository configuration
- `.claude/`: local Claude configuration

## Notes for Claude

- Keep backend changes inside `BE/` unless repository-level configuration is required.
- Keep frontend changes inside `FE/` unless repository-level configuration is required.
- Do not commit local Claude settings such as `.claude/settings.local.json`.
- Prefer existing project conventions and focused changes.
